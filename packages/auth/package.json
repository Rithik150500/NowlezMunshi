{
  "name": "@nowlez/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Authentication & identity — an AuthService supporting phone OTP, email + password, and Google sign-in over ports, with offline fakes and Node-scrypt password hashing.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@nowlez/contracts": "workspace:*"
  },
  "devDependencies": {
    "@nowlez/persistence": "workspace:*"
  }
}

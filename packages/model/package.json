{
  "name": "@nowlez/model",
  "version": "0.0.0",
  "private": true,
  "description": "ModelClient adapters (ADR-0009): a deterministic fake for tests and an env-driven OpenAI-compatible client for real Gemma endpoints (self-host or hosted). Keys come from the environment, never committed.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@nowlez/contracts": "workspace:*"
  }
}

{
  "name": "@nowlez/storage",
  "version": "0.0.0",
  "private": true,
  "description": "BlobStore adapters (ADR-0014): in-memory (default / tests) and a filesystem store for the bytes a BinaryRef points at (e.g. write_docx output).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@nowlez/contracts": "workspace:*"
  }
}
